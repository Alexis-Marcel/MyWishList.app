{% extends 'templates/app.twig' %}

{% block content %}

    <h2 class="mt-4">Add an item</h2>
    <form action="{{path_for('edit.additem',{'secureToken' : value.token})}}" method="post" autocomplete="off">
        <table class="table">
            <thead>
            <tr>
                <th scope="col">Name</th>
                <th scope="col">Description</th>
                <th scope="col">Image</th>
                <th scope="col">Price</th>
            </tr>
            </thead>
            <tbody>
            <tr>
                <td>
                    <div class="form-group">
                        <input type="text" name="name" class="form-control {{ errors.name ? 'is-invalid' : '' }}" placeholder="Name of the item" id="name" autocomplete="off" value="{{ old.name }}">
                        <div class="invalid-feedback">
                            {{ errors.name | first }}
                        </div>
                    </div>
                </td>
                <td>
                    <div class="form-group">
                        <input type="text" name="descr" class="form-control {{ errors.descr ? 'is-invalid' : '' }}" placeholder="Description of the item" id="descr" autocomplete="off" value="{{ old.descr }}">
                        <div class="invalid-feedback">
                            {{ errors.descr | first }}
                        </div>
                    </div>
                </td>
                <td>
                    <div class="form-group">
                        <input type="text" name="image" class="form-control {{ errors.image ? 'is-invalid' : '' }}" placeholder="Image of the item" id="image" autocomplete="off" value="{{ old.image }}">
                        <div class="invalid-feedback">
                            {{ errors.image | first }}
                        </div>
                    </div>
                </td>
                <td>
                    <div class="form-group">
                        <input type="number" step="any" name="price" class="form-control {{ errors.price ? 'is-invalid' : '' }}" placeholder="Price of the item" id="price" autocomplete="off" value="{{ old.price }}">
                        <div class="invalid-feedback">
                            {{ errors.price | first }}
                        </div>
                    </div>
                </td>
            </tr>

            </tbody>
        </table>
        <table class="table">
            <thead>
            <tr>
                <th scope="col">External url (optional)</th>
            </tr>
            </thead>
            <tbody>
            <tr>
                <td>
                    <div class="form-group">
                        <input type="text" name="url" class="form-control {{ errors.url ? 'is-invalid' : '' }}" placeholder="Link to an external site" id="url" autocomplete="off" value="{{ old.url }}">
                        <div class="invalid-feedback">
                            {{ errors.url | first }}
                        </div>
                    </div>
                </td>
            </tr>
            </tbody>
        </table>
        <div class="float-end">
            <input id="add-btn" type="submit" class="btn mt-4 align-items-end" value="Add"/>
        </div>
        {{ csrf.field | raw}}
    </form>
{% endblock %}